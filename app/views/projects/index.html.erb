<h1>Projects</h1>
<%= link_to "New Project", new_project_path %>
<ul>
  <% @projects.each do |project| %>
    <li><%= link_to project.title, project_path(project) %> by <%= project.owner.name %></li>
  <% end %>
</ul>
