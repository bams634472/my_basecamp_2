<%= form_with model: @project, local: true do |f| %>
  <div><%= f.label :title %><br><%= f.text_field :title, required: true %></div>
  <div><%= f.label :description %><br><%= f.text_area :description %></div>
  <div><%= f.submit %></div>
<% end %>
